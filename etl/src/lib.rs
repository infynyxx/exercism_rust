pub fn transform(input: &[(i32, Vec<String>)]) -> [(i32, String)] {
    let length = input.len();
    println!("length={}", length);
    let mut transformed_arr: Vec<(i32, String)> = Vec::with_capacity(length);
    transformed_arr
}
